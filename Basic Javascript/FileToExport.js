const pi=3.14;
function area(radius){
    return pi*radius*radius;
}

class Choclate{
     constructor(flavour,color){
         this.flavour=flavour
         this.color=color
     }
};

module.pi = pi;